//! Lookup receipt — result of an engram lookup operation.
//!
//! Re-exports the canonical `EngramLookupReceipt` type from the training
//! target spec, providing a stable path for runtime consumers.

pub use crate::ecs::training_target::spec::EngramLookupReceipt;
