//! `chapter_presentation_system` — orders chapters for the route.

use prism_ecs_core::World;

use crate::error::RuntimeError;

pub fn run(_world: &mut World) -> Result<(), RuntimeError> {
    Ok(())
}
