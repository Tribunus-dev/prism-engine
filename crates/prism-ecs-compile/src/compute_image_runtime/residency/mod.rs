//! Compiled residency plan types — placeholder (filled in by migration
//! sub-step).

// TODO(migration): migrate data-only files from the engine's
// `compute-core/src/ecs/compute_image/residency/` directory. Currently
// a placeholder stub.
