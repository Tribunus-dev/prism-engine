//! Canonical identity types for kernel semantics and implementations.
//!
//! These types provide stable identifiers used across the compile pipeline
//! and runtime for mapping operations to their hardware implementations.

pub mod kernel_abi;
