//! Engine core surface — placeholder.
//!
//! The constitutional home for the engine's `compute-core/src/ecs/core/`
//! subsystem. The 121 files of the legacy engine core are scheduled for
//! re-homing to this module as the engine-absorption wave progresses.
//!
//! As of the initial commit, the engine's `core/` directory still exists
//! and is unchanged; the `prism_ecs_core::core` surface is a passthrough
//! index. Subsequent commits move individual files here with a one-sentence
//! module doc stating the single authority each file owns, until the
//! engine's `compute-core/src/ecs/core/` directory can be deleted entirely.
//!
//! See `changelogs/2026-07-27-engine-subsystem-deletion-core.md` for the
//! migration goal and the per-file authority inventory.
