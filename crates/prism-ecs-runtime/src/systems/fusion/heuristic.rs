//! Canonical authority for the fusion heuristic system type that scores fusion candidates against the backend capability registry and the fusion policy.

pub struct FusionHeuristicSystem;

impl Default for FusionHeuristicSystem {
    fn default() -> Self { Self }
}
