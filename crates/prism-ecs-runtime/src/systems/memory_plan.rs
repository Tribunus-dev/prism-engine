//! Canonical authority for memory-planning system types that assign each tensor to a memory domain and allocate backing buffers.

pub struct MemoryDomainAssignmentSystem;

pub struct BufferAllocationSystem;
