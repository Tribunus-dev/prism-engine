//! Canonical authority for the CImage assembly and receipt-signing system types that finalize the artifact and the durable evidence.

pub struct CImageAssemblySystem;

pub struct ReceiptSigningSystem;
