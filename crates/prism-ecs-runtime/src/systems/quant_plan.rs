//! Canonical authority for codec-selection and precision-plan system types that decide the runtime representation class per tensor.

pub struct CodecSelectionSystem;

pub struct PrecisionPlanSystem;
