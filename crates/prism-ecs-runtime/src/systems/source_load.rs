//! Canonical authority for the source-loading system types that load safetensors shards, attach shape / dtype / source-meta components, and load the tensor hash table.

pub struct SourceLoadingSystem;

pub struct TensorTableLoadingSystem;

pub struct SourceTensorMeta;

pub struct DiffSystem;

pub struct TensorTableComp;
