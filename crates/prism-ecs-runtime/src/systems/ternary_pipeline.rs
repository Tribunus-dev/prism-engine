//! Canonical authority for the tertiary pipeline system type that runs the canonical ternary quantization pipeline end-to-end.

pub struct TertiaryPipelineSystem;

impl Default for TertiaryPipelineSystem {
    fn default() -> Self { Self }
}

impl TertiaryPipelineSystem {
    pub fn new() -> Self { Self }
}
