//! Canonical authority for the auto-tuning and AOT profile-match system types that select tuned kernel parameters for a target backend. The engine's compile_session.rs references these; the engine file is no longer present in the engine source.

pub struct AutoTuningSystem;

pub struct AOTProfileMatchSystem;
