{
  "name": "prism-engine-site-audit",
  "private": true,
  "version": "0.0.0",
  "description": "Prism website diagnostics and reproduction tooling",
  "scripts": {
    "matrix:check": "playwright test prism-matrix.spec.js",
    "matrix:chrome": "playwright test --project=chrome prism-matrix.spec.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "playwright": "^1.61.1"
  }
}
