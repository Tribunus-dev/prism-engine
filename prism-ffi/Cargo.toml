[package]
name = "prism-ffi"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["staticlib"]

[dependencies]

[features]
default = []
ios = []

[package.metadata]
docs = "C ABI bridge for the Prism Engine Swift menu bar app (stub only)"
