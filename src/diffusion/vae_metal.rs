#[cfg(target_os = "macos")]
pub struct VaeMetalPipeline {
    // Placeholder implementation for Metal shader bindings for VAE decoder.
}

#[cfg(target_os = "macos")]
impl VaeMetalPipeline {
    pub fn new() -> Self {
        Self {}
    }
}
